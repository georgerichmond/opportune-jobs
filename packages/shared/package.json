{
  "name": "@opportune-jobs/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for opportune-jobs monorepo",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
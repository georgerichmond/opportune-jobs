# Database Configuration
# Copy this file to .env.local and update values as needed

# PostgreSQL Configuration (used by docker-compose.yml in packages/database/)
POSTGRES_USER=opportune_dev
POSTGRES_PASSWORD=password123
POSTGRES_DB=opportune_jobs_dev

# Database Connection String for Applications
DATABASE_URL=postgresql://opportune_dev:password123@localhost:5432/opportune_jobs_dev

# For Railway deployment, this will be automatically injected by Railway
# DATABASE_URL=postgresql://user:password@host:port/database
